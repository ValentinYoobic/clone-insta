import { Meta, StoryObj } from '@storybook/angular';
import { PostCommentsComponent } from './post-comments.component';

const meta: Meta<PostCommentsComponent> = {
  title: 'Components/Post Comments',
  component: PostCommentsComponent,
};

export default meta;
type Story = StoryObj<PostCommentsComponent>;

export const PostComments: Story = {
};
